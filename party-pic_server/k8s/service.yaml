apiVersion: v1
kind: Service
metadata:
  name: party-pic-server
  labels:
    app: party-pic-server
spec:
  type: ClusterIP
  selector:
    app: party-pic-server
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
