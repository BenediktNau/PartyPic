# =============================================================================
# PARTY-PIC SERVER HELM CHART
#
# Helm Chart fuer das NestJS Backend.
# Deployed Deployment, Service, Ingress, HPA, PDB und ServiceMonitor.
# =============================================================================
apiVersion: v2
name: party-pic-server
description: A Helm chart for the party pic server
type: application
version: 0.1.0
appVersion: "1.0.0"