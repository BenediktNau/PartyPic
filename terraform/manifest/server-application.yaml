apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: party-pic-server-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/BenediktNau/PartyPic.git
    targetRevision: elias
    path: party-pic_server/chart

  destination:
    server: https://kubernetes.default.svc
    namespace: party-pic

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
